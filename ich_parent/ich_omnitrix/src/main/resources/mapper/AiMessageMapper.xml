<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.omnitrix.mapper.AiMessageMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_message (conversation_id, session_id, role, content, tokens, model, sub_agent, latency_ms)
        VALUES (#{conversationId}, #{sessionId}, #{role}, #{content}, #{tokens}, #{model}, #{subAgent}, #{latencyMs})
    </insert>

    <select id="selectByConversationId" resultType="com.hyang.ich.omnitrix.entity.AiMessage">
        SELECT * FROM ai_message
        WHERE conversation_id = #{conversationId}
        ORDER BY create_time ASC
    </select>

    <select id="selectBySessionId" resultType="com.hyang.ich.omnitrix.entity.AiMessage">
        SELECT * FROM ai_message
        WHERE session_id = #{sessionId}
        ORDER BY create_time ASC
    </select>

    <select id="countByConversationId" resultType="int">
        SELECT COUNT(*) FROM ai_message WHERE conversation_id = #{conversationId}
    </select>

    <select id="selectLastBySessionIdAndRole" resultType="com.hyang.ich.omnitrix.entity.AiMessage">
        SELECT * FROM ai_message
        WHERE session_id = #{sessionId} AND role = #{role}
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <delete id="deleteById">
        DELETE FROM ai_message WHERE id = #{id}
    </delete>

</mapper>
