<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.content.mapper.product.InventoryRecordMapper">

    <sql id="allColumns">
        id, product_id, product_name, type, quantity, before_stock, after_stock, reason,
        operator_id, operator_name, create_time, update_time
    </sql>

    <sql id="condition">
        <where>
            is_deleted = 0
            <if test="keyword != null and keyword != ''">AND (product_name LIKE CONCAT('%',#{keyword},'%') OR reason LIKE CONCAT('%',#{keyword},'%'))</if>
            <if test="type != null">AND type = #{type}</if>
        </where>
    </sql>

    <select id="selectByCondition" resultType="com.hyang.ich.content.entity.InventoryRecord">
        SELECT <include refid="allColumns"/> FROM inventory_record
        <include refid="condition"/>
        ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*) FROM inventory_record <include refid="condition"/>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO inventory_record (product_id, product_name, type, quantity, before_stock, after_stock, reason, operator_id, operator_name)
        VALUES (#{productId}, #{productName}, #{type}, #{quantity}, #{beforeStock}, #{afterStock}, #{reason}, #{operatorId}, #{operatorName})
    </insert>

    <update id="deleteById">
        UPDATE inventory_record SET is_deleted = 1 WHERE id = #{id}
    </update>
</mapper>
