<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.content.mapper.content.IchActivityViewLogMapper">

    <sql id="columns">
        id, activity_id, user_id, user_name, view_time
    </sql>

    <select id="selectByActivityId" resultType="com.hyang.ich.content.entity.IchActivityViewLog">
        SELECT <include refid="columns"/> FROM ich_activity_view_log
        WHERE activity_id = #{activityId}
        ORDER BY view_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countByActivityId" resultType="int">
        SELECT COUNT(*) FROM ich_activity_view_log
        WHERE activity_id = #{activityId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ich_activity_view_log (activity_id, user_id, user_name)
        VALUES (#{activityId}, #{userId}, #{userName})
    </insert>
</mapper>
