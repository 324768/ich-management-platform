<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.content.mapper.content.IchItemMapper">

    <sql id="columns">
        id, category_id, name, level, region_code, region_name,
        declaration_unit, protection_unit, declaration_time, description, content,
        status, sort, create_time, update_time
    </sql>

    <sql id="allColumns">
        id, category_id, name, cover_image, level, region_code, region_name,
        declaration_unit, protection_unit, declaration_time, description, content,
        detail_images, videos, status, sort, create_time, update_time
    </sql>

    <sql id="condition">
        <where>
            is_deleted = 0
            <if test="categoryId != null">AND category_id = #{categoryId}</if>
            <if test="keyword != null and keyword != ''">AND (name LIKE CONCAT('%',#{keyword},'%') OR description LIKE CONCAT('%',#{keyword},'%'))</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </sql>

    <select id="selectByCondition" resultType="com.hyang.ich.content.entity.IchItem">
        SELECT <include refid="allColumns"/> FROM ich_item
        <include refid="condition"/>
        ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*) FROM ich_item <include refid="condition"/>
    </select>

    <select id="selectById" resultType="com.hyang.ich.content.entity.IchItem">
        SELECT <include refid="allColumns"/> FROM ich_item WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ich_item (category_id, name, cover_image, level, region_code, region_name,
            declaration_unit, protection_unit, declaration_time, description, content, detail_images, videos, status, sort)
        VALUES (#{categoryId}, #{name}, #{coverImage}, #{level}, #{regionCode}, #{regionName},
            #{declarationUnit}, #{protectionUnit}, #{declarationTime}, #{description}, #{content}, #{detailImages}, #{videos}, #{status}, #{sort})
    </insert>

    <update id="update">
        UPDATE ich_item
        <set>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="level != null">level = #{level},</if>
            <if test="regionCode != null">region_code = #{regionCode},</if>
            <if test="regionName != null">region_name = #{regionName},</if>
            <if test="declarationUnit != null">declaration_unit = #{declarationUnit},</if>
            <if test="protectionUnit != null">protection_unit = #{protectionUnit},</if>
            <if test="declarationTime != null">declaration_time = #{declarationTime},</if>
            <if test="description != null">description = #{description},</if>
            <if test="content != null">content = #{content},</if>
            <if test="detailImages != null">detail_images = #{detailImages},</if>
            <if test="videos != null">videos = #{videos},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sort != null">sort = #{sort},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE ich_item SET is_deleted = 1 WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE ich_item SET status = #{status} WHERE id = #{id} AND is_deleted = 0
    </update>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM ich_item WHERE is_deleted = 0
    </select>

</mapper>
