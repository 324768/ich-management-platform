<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.content.mapper.content.IchActivityCommentMapper">

    <sql id="columns">
        id, activity_id, user_id, user_name, user_avatar, content, status, create_time, update_time
    </sql>

    <select id="selectByActivityId" resultType="com.hyang.ich.content.entity.IchActivityComment">
        SELECT <include refid="columns"/> FROM ich_activity_comment
        WHERE activity_id = #{activityId} AND is_deleted = 0
        ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countByActivityId" resultType="int">
        SELECT COUNT(*) FROM ich_activity_comment
        WHERE activity_id = #{activityId} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ich_activity_comment (activity_id, user_id, user_name, user_avatar, content, status)
        VALUES (#{activityId}, #{userId}, #{userName}, #{userAvatar}, #{content}, #{status})
    </insert>

    <update id="deleteById">
        UPDATE ich_activity_comment SET is_deleted = 1 WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE ich_activity_comment SET status = #{status} WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>
