<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.content.mapper.content.VideoExhibitionMapper">

    <sql id="allColumns">
        id, title, cover_url, video_url, duration, category_id, description, content,
        view_count, like_count, status, sort, create_time, update_time
    </sql>

    <select id="selectAll" resultType="com.hyang.ich.content.entity.VideoExhibition">
        SELECT <include refid="allColumns"/> FROM video_exhibition WHERE is_deleted = 0 ORDER BY sort ASC, create_time DESC
    </select>

    <select id="selectByStatus" resultType="com.hyang.ich.content.entity.VideoExhibition">
        SELECT <include refid="allColumns"/> FROM video_exhibition WHERE is_deleted = 0 AND status = #{status} ORDER BY sort ASC, create_time DESC
    </select>

    <select id="selectById" resultType="com.hyang.ich.content.entity.VideoExhibition">
        SELECT <include refid="allColumns"/> FROM video_exhibition WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO video_exhibition (title, cover_url, video_url, duration, category_id, description, content, status, sort)
        VALUES (#{title}, #{coverUrl}, #{videoUrl}, #{duration}, #{categoryId}, #{description}, #{content}, #{status}, #{sort})
    </insert>

    <update id="update">
        UPDATE video_exhibition
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="coverUrl != null">cover_url = #{coverUrl},</if>
            <if test="videoUrl != null">video_url = #{videoUrl},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="description != null">description = #{description},</if>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sort != null">sort = #{sort},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE video_exhibition SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM video_exhibition WHERE is_deleted = 0
    </select>
</mapper>
