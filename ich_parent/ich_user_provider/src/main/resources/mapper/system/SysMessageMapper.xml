<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.user.mapper.system.SysMessageMapper">

    <sql id="allColumns">
        id, title, content, message_type, receiver_type, receiver_ids, is_published,
        publish_time, expire_time, create_by, create_time, update_time
    </sql>

    <sql id="condition">
        <where>
            is_deleted = 0
            <if test="keyword != null and keyword != ''">AND (title LIKE CONCAT('%',#{keyword},'%') OR content LIKE CONCAT('%',#{keyword},'%'))</if>
            <if test="messageType != null">AND message_type = #{messageType}</if>
            <if test="isPublished != null">AND is_published = #{isPublished}</if>
        </where>
    </sql>

    <select id="selectByCondition" resultType="com.hyang.ich.user.entity.SysMessage">
        SELECT <include refid="allColumns"/> FROM sys_message
        <include refid="condition"/>
        ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*) FROM sys_message <include refid="condition"/>
    </select>

    <select id="selectById" resultType="com.hyang.ich.user.entity.SysMessage">
        SELECT <include refid="allColumns"/> FROM sys_message WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_message (title, content, message_type, receiver_type, receiver_ids, is_published, publish_time, expire_time, create_by)
        VALUES (#{title}, #{content}, #{messageType}, #{receiverType}, #{receiverIds}, #{isPublished}, #{publishTime}, #{expireTime}, #{createBy})
    </insert>

    <update id="update">
        UPDATE sys_message
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="messageType != null">message_type = #{messageType},</if>
            <if test="receiverType != null">receiver_type = #{receiverType},</if>
            <if test="receiverIds != null">receiver_ids = #{receiverIds},</if>
            <if test="isPublished != null">is_published = #{isPublished},</if>
            <if test="publishTime != null">publish_time = #{publishTime},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE sys_message SET is_deleted = 1 WHERE id = #{id}
    </update>

    <update id="publish">
        UPDATE sys_message SET is_published = 1, publish_time = NOW() WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>
