<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.user.mapper.system.SysRoleMapper">

    <select id="selectAll" resultType="com.hyang.ich.user.entity.SysRole">
        SELECT id, name, code, description, sort, status, create_time, update_time
        FROM sys_role WHERE is_deleted = 0 ORDER BY sort ASC
    </select>

    <select id="selectById" resultType="com.hyang.ich.user.entity.SysRole">
        SELECT id, name, code, description, sort, status, create_time, update_time
        FROM sys_role WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (name, code, description, sort, status)
        VALUES (#{name}, #{code}, #{description}, #{sort}, #{status})
    </insert>

    <update id="update">
        UPDATE sys_role
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="code != null">code = #{code},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE sys_role SET is_deleted = 1 WHERE id = #{id}
    </update>
</mapper>
