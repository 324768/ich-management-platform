<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyang.ich.user.mapper.order.OrderItemMapper">

    <sql id="itemColumns">
        id, order_id, order_no, product_id, product_pic, product_name, product_brand,
        product_sn, product_price, product_quantity, product_sku_id, product_sku_code,
        product_category_id, create_time, update_time
    </sql>

    <insert id="insertBatch">
        INSERT INTO order_item (order_id, order_no, product_id, product_pic, product_name, product_brand,
            product_sn, product_price, product_quantity, product_sku_id, product_sku_code, product_category_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.orderNo}, #{item.productId}, #{item.productPic},
             #{item.productName}, #{item.productBrand}, #{item.productSn}, #{item.productPrice},
             #{item.productQuantity}, #{item.productSkuId}, #{item.productSkuCode}, #{item.productCategoryId})
        </foreach>
    </insert>

    <select id="selectByOrderId" resultType="com.hyang.ich.user.entity.OrderItem">
        SELECT <include refid="itemColumns"/> FROM order_item WHERE order_id = #{orderId} AND is_deleted = 0
    </select>

    <select id="selectByOrderNo" resultType="com.hyang.ich.user.entity.OrderItem">
        SELECT <include refid="itemColumns"/> FROM order_item WHERE order_no = #{orderNo} AND is_deleted = 0
    </select>
</mapper>
